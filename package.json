{
  "name": "adlibere",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Automatic streaming-site popup, overlay and anti-adblock countermeasures.",
  "scripts": {
    "build": "node tools/build.mjs",
    "pretest": "npm run build",
    "test": "node --test \"tests/*.test.mjs\""
  },
  "engines": {
    "node": ">=24"
  }
}
